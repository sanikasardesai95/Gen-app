apiVersion: apps/v1
kind: Deployment
metadata:
  name: text-generator-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: text-generator
  template:
    metadata:
      labels:
        app: text-generator
    spec:
      containers:
      - name: smart-hire
        image: gcr.io/kunal-ica1/text-generator:latest
        ports:
        - containerPort: 8080
